# Backend/

Staging area for **new** backend / engine experiments that aren't yet wired into
the existing pipeline. There is no runtime service here.

Early-analysis notebooks and scripts moved to
[`ARCHIVE/EARLY Analysis/`](../ARCHIVE/EARLY%20Analysis/).

The active physics engines live under `Physics Engine/` (`SIM` / `SIM3D`
symlinks at the repo root). See [`../ARCHIVE/README.md`](../ARCHIVE/README.md)
for provenance layout.
