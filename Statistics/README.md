# Statistics

Statistics tab assets.

| File | Role |
|------|------|
| `spectrum.js` / `spectrum.css` | Spectrum analyzer panel |

Histogram / Antenna Pattern markup still lives in `Frontend_Data_Display.html` (`#histTab`);
plotting is driven by `Map Coverage/dashboard.js` (shared Plotly helpers). Further splits
can move those modules here later.
