# Frontend

App chrome that is not a dashboard tab:

| Path | Role |
|------|------|
| `landing/` | Indoor/Outdoor · 2D/3D router and workspace flow |
| `indoor-outdoor/` | Related landing helpers |

Tab modules live at the repo root next to this folder:

- `Map Coverage/`
- `Statistics/`
- `Time Elapse/`
- `Simulation/`

Open `Frontend_Data_Display.html` from the repo root over HTTP.
